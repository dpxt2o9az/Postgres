package com.example.postgres;

public class PsuedoCode {

    public static void main(String[] args) {
        // create connection
        // insert new parent record
        // fetch the insertion id
        // insert new child records with parent's insertion id
    }
}
